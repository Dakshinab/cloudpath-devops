apiVersion: v1
kind: Service
metadata:
  name: cloudpath-service
  namespace: cloudpath
spec:
  type: NodePort
  selector:
    app: cloudpath-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
